export enum EventState {
  DRAFT = 'draft',
  RESQUESTED = 'resquested',
  IN_REVIEW = 'in_review',
  SCHEDULED = 'scheduled',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
  HISTORICAL = 'historical'
}
