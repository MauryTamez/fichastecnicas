export enum EventState {
  DRAFT = 'draft',
  REQUESTED = 'requested',
  IN_REVIEW = 'in_review',
  SCHEDULED = 'scheduled',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
  HISTORICAL = 'historical'
}
